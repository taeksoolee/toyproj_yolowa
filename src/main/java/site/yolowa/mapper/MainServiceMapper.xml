<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.yolowa.mapper.MainServiceMapper">
	<select id="selectMainServiceTerms" resultType="Board">
		select * from board where board_category=#{boardCategory} and board_type=3
	</select>
</mapper>